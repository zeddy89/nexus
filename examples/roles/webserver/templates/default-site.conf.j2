# Default site configuration - managed by Nexus
server {
    listen {{ nginx_port }} default_server;
    listen [::]:{{ nginx_port }} default_server;

    root {{ web_root }};
    index index.html index.htm;

    server_name {{ server_name }};

    location / {
        try_files $uri $uri/ =404;
    }
}
